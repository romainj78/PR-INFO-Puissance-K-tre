unit choixMode;

interface 

uses types;

const MODE_UNCONTREUN = 1;
const MODE_SURPRISE = 2;
const MODE_SOLO = 3;
const MODE_PUISSANCEN = 4;
const MODE_SOLO_DIFF = 5;

procedure choixMode(var modeJeu: Integer);

implementation

procedure choixMode(var modeJeu: Integer);
begin
    
end;

end.